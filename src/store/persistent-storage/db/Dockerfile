# Use the official PostgreSQL image from Docker Hub
FROM postgres:latest

# Environment variables for PostgreSQL configuration 

# ENV POSTGRES_DB       # hydrated at deployment via manifest
# ENV POSTGRES_USER     # hydrated at deployment via manifest
# ENV POSTGRES_PASSWORD # hydrated at deployment via manifest

# Expose the PostgreSQL port
EXPOSE 5432